# Dokploy access for deploy/dokploy.sh. Copy to deploy/dokploy.env (gitignored)
# and fill in. The key comes from Dokploy → Settings → API/CLI.
DOKPLOY_API=https://server.codeation.io/api
DOKPLOY_KEY=
DOKPLOY_COMPOSE_ID=
DOKPLOY_APP=
